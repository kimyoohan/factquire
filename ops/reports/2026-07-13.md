# Weekly refresh verification — 2026-07-13

Reviewer: Claude (independent cross-verification per VERIFY_ORDER).
Collector: Codex UPDATE_ORDER run 2026-07-13 (commit ce28462).

## Preconditions
- `Weekly refresh 2026-07-13` commit present (ce28462); `validate.py` exit 0 (135 entries, 16 providers, 722 logic-audited fields).
- Changelog v0.7 "No changes; all facts re-verified" (0 entries).
- **Independent archive-vs-current diff (data/archive/facts-2026-07-07.json → facts.json, ignoring timestamps/sources):**
  - 32 `pricing` diffs are all cosmetic float→int normalization (`5.0`→`5`, `10.0`→`10`) — **zero value change**.
  - **25 substantive `None`→value fills** in context_window/max_output (Gemini family 20, MiniMax 5, Amazon claude-3-5-sonnet 1). These landed mid-week in commit 5f08780 (2026-07-08, "fill 25 verified context/max-output fields") — i.e. changed *this week* but before the 07-13 refresh baseline, so the changelog legitimately reads "no changes since prior state." Flagged for verification because they are the freshest public facts and were last week's stated focus.

## Sample (5 entries, 5 providers — raw-HTML/API only, no summarizing fetch tools)
Rotation: last week sampled {groq, deepseek, cohere, anthropic, openai}; this week rotated onto the fill set + {google, fireworks, together} per prior note.

| provider | model | field checked | our value | primary source (raw HTML) | evidence snippet | result |
|---|---|---|---|---|---|---|
| google | gemini-2.5-pro | context / max_output | 1,048,576 / 65,536 | ai.google.dev/gemini-api/docs/models/gemini-2.5-pro | raw table: `1,048,576` + `65,536` under `Input/Output token limit` | ✅ |
| amazon | anthropic.claude-3-5-sonnet-20241022-v2:0 | max_output (fill) / context | 8,192 / 200,000 | platform.claude.com release notes + anthropic.com | raw: "Generate outputs up to **8,192** tokens in length"; `200K` present | ✅ |
| minimax | minimax-m3 | context (fill) | 1,000,000 | platform.minimax.io/docs/api-reference/text-openai-api | raw table cell `MiniMax-M3</td>…1,000,000`; M2.1/2.5/2.7 → `204,800` (also matches) | ✅ |
| fireworks | kimi-k2.6 | input / cached / output | 0.95 / 0.16 / 4.00 | docs.fireworks.ai/serverless/pricing | raw table: `Kimi K2.6</a></td>…$0.95 / $0.16 / $4.00` — exact 3/3 | ✅ |
| together | minimax-m3 | input / cached / output | 0.30 / 0.06 / 1.20 | together.ai/pricing | raw: `MiniMax-M3` row `$0.30`; `0.06` + `1.20` present on page | ✅ |

Inconclusive (JS SPA, values not in raw HTML — NOT a fail, consistent with prior weeks):
- perplexity docs.perplexity.ai (Mintlify SPA) — sonar-pro 200K/pricing not in raw response.
- mistral mistral.ai/pricing/api/ (SPA) — token prices not in raw HTML (same as 2026-07-07 note).

## Verdict: ALL PASS
Deployed via `publish.py`: feed.json rebuilt (135 models), pushed (8b4f717), GitHub Pages deploy confirmed (2 checks), IndexNow 200 OK for /feed.json. Live `factquire.com/feed.json` HTTP 200, `generated_at 2026-07-13T01:30:01Z`.

## Story drafts
No `content/drafts/story-*.md` this cycle — no stories.

## Notes for next week
- **Changelog-completeness gap (process, not data):** the 2026-07-08 fill of 25 context/max_output fields (commit 5f08780) never got its own changelog release entry, so a reader diffing only changelog releases would miss 25 real new facts. Consider having UPDATE_ORDER/publish emit a changelog release for any mid-week substantive fill, or have the weekly refresh reconcile against the last *archive* (not last commit) so intra-week fills surface in v0.x notes.
- Verified fills confirm Codex paid down some evidence debt on context/max_output (Gemini/MiniMax/Amazon now populated). Baseline UNSUPPORTED trend still worth watching next UPDATE_ORDER (last week 185).
- Rotate next week's unchanged sample off {google, amazon, minimax, fireworks, together}; try {zhipu, alibaba, cohere, moonshot, xai}. Prefer server-rendered sources (skip perplexity/mistral/together-SPA for value-critical checks or fetch their embedded JSON payloads).
